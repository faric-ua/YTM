# Termux — v1.4.20 QA + UI fix

This package is an additive overlay.

It records the successful v1.4.19 phone bulk-export run, adds sanitized evidence,
bumps the app to v1.4.20, and fixes the clipped/too-tight account action buttons.

Use the exact Termux block supplied by ChatGPT. Do not delete old QA folders.
