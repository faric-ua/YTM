# YTM Importer v1.4.14

Session recovery + result modal + master QA plan.

Key changes:
- in-place update/restart can silently recover a previously granted Google/YTM authorization;
- OAuth access token is still never persisted by the app;
- playlist completion result is now a modal with Open / Copy / Close;
- global manual QA lives in `qa/MASTER_TEST_PLAN.md`.

Test status: NOT TESTED YET.
