# v1.4.24 — Phone Test

Use **Blue Dark first**, because the current v1.4.23 phone evidence is already Blue.

## 1. Import

Open `1. Імпорт`.

Expected:
- background/card/button colors are Blue Dark;
- no old graphite/red fixed styling dominates;
- account export/import buttons still fit.

Send one screenshot.

## 2. Review

Open `3. Знайти / перевірити` for the current exact project.

Expected:
- summary/card/buttons follow Blue Dark;
- track states remain readable;
- exact tracks stay ready.

Send one screenshot.

## 3. Destination

Open `4. Створити / додати`.

Expected:
- cards/buttons follow Blue Dark;
- no write occurs until explicitly confirmed.

Send one screenshot.

## 4. Utility screens

Open:
- Історія
- Черга
- Дані
- Сервіс

Expected:
- each screen follows Blue Dark;
- Service nested page stays themed;
- Back behavior is unchanged.

Two or three screenshots are enough if the style is clearly consistent.

## 5. Theme spot-check

Switch to Neon or Green and reopen one utility screen.

Expected:
- that screen follows the newly selected palette immediately.
