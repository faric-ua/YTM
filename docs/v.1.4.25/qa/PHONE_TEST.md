# v1.4.25 — Phone Test

## Перевірка 1 — Home

Expected:
- `4 кроки` card has two strokes;
- `Поточний плейлист` now has the same two-stroke treatment;
- track cards also have two strokes;
- small `Історія / Черга / Квота / Ще` controls remain visually quiet.

Send one screenshot.

## Перевірка 2 — Destination

Open `Створити / додати`.

Expected:
- large cards have the two-stroke accent;
- selected privacy radio follows the active theme (blue in Blue Dark);
- no write occurs.

Send one screenshot.

## Перевірка 3 — History + Queue

Expected:
- large cards have accent strokes;
- hints are short and fully visible:
  - `Пошук історії`
  - `Пошук у черзі`

One or two screenshots are enough.

## Перевірка 4 — Data

Expected:
- ordinary large cards use theme accent;
- `Безпека` keeps amber top/bottom strokes.
