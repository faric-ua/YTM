# v1.4.9 — Before / After

```text
ACCOUNT
Before: connected → rotate → Step 2 disconnected
After:  connected → memory session → rotate → Step 2 stays connected

DIALOG
Before: centered provisional frame → visible jump → final position
After:  hidden provisional frame → final insets → first visible final position
```
