# YTM Importer v1.4.41-R1

- versionName: **1.4.41-R1**
- versionCode: **78**
- status: **NOT PHONE-TESTED YET**
- scope: **two targeted corrective phone findings only**

## Fix 1 — BUG-011 Account modal survives rotation

Installed v1.4.41 phone reproduction:

Path:
`Home → 2. Google / YTM → Account modal → rotate phone`

Actual:
- Account modal displayed correctly in portrait;
- after portrait → landscape rotation the modal disappeared.

R1 implementation:
- store an `accountDialogOpen` flag in `onSaveInstanceState`;
- restore it during Activity recreation;
- reopen the Account modal after the recreated Home window is ready;
- clear the flag whenever the dialog is actually dismissed;
- preserve existing account/session data and button order.

## Fix 2 — UX-017 duplicate-download filename suffix

Installed v1.4.41 phone reproduction:

A fallback-only file downloaded more than once was named like:

`House_Dance_Hit_2000_Vol1_YTM-1.txt`

Observed title:

`House Dance Hit 2000 Vol.1 YTM-1`

Expected:

`House Dance Hit 2000 Vol.1`

R1 implementation extends filename cleanup so the trailing service marker may also
carry common download-copy suffixes, including:

- `YTM-1`
- `YTM_1`
- `YTM (1)`

Explicit in-file playlist titles remain authoritative.

## Phone acceptance

R1 is accepted only after both targeted checks pass on the real phone:

1. `Home → 2. Google / YTM → rotate portrait → landscape → portrait`
   - Account modal remains/reappears after both rotations;
   - `Змінити` remains left and `Закрити` right.
2. Import fallback-only duplicate files:
   - `House_Dance_Hit_2000_Vol1_YTM-1.txt`
   - expected title `House Dance Hit 2000 Vol.1`.

No broad regression PASS is implied by these two corrective checks.
