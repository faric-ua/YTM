# YTM Importer v1.4.41-R2

- versionName: **1.4.41-R2**
- versionCode: **79**
- status: **PHONE RETEST PASS — UX-017 CLOSED**
- scope: **stacked duplicate-download filename suffix only**

## Why R2 exists

R1 phone results:
- version badge: PASS;
- BUG-011 Account modal rotation: PASS / closed;
- simple duplicate suffix `YTM-1`: PASS;
- stacked duplicate suffix `YTM-1 (1)`: FAIL.

Observed title:

`House Dance Hit 2000 Vol.1 YTM-1 (1)`

Expected:

`House Dance Hit 2000 Vol.1`

## R2 correction

The filename normalizer previously accepted at most one duplicate-copy suffix token
after `YTM`.

R2 accepts repeated suffix tokens, so forms such as these are stripped:

- `YTM-1 (1)`
- `YTM (1) (2)`
- `YTM_1 (1)`

Explicit in-file titles remain authoritative.

## Phone acceptance

Path:
`Home → 1. Імпорт → імпортувати файл`

Import the already-existing fallback-only duplicate file that previously produced
`House Dance Hit 2000 Vol.1 YTM-1 (1)`.

PASS:
- displayed playlist title is exactly `House Dance Hit 2000 Vol.1`;
- no Search API call is needed.

No broader regression claim is implied by R2.


## Phone result

Real-phone v1.4.41-R2 result: **PASS**.

Confirmed:
- version badge shows `v1.4.41-R2`;
- simple duplicate suffix `YTM-1` resolves correctly;
- stacked `YTM-1 (1)` resolves correctly;
- deeper stacked form `YTM-1 (1) (1)` also resolves correctly;
- displayed playlist title is exactly:
  `House Dance Hit 2000 Vol.1`.

UX-017 is closed on v1.4.41-R2.

No Search API call was required for this acceptance.
