# v1.4.20 — Phone UI Smoke

Use simple test wording with the user:

## Перевірка 1 — кнопки імпорту

Open **1. Імпорт**.

Expected:
- both YTM buttons are completely readable;
- bulk-export text is not cut off;
- visible spacing exists between the two buttons.

Send one screenshot.

## Перевірка 2 — короткий функціональний smoke

Tap **Експортувати всі плейлисти в папку**.

Expected:
- Android folder picker still opens.

It is not necessary to repeat the entire 21-playlist evidence run unless the UI fix unexpectedly affects the flow.
