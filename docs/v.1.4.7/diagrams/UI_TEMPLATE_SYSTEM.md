# v1.4.7 — UI template system

```text
Confirmation:
  short message + text actions

Record dialog:
  title + card per record + stacked export actions + flat close

Service detail:
  top bar + scrollable section cards + full-width actions

Navigation card:
  title + subtitle + chevron
```
