# v1.4.44-R1 — Landscape Copy Fit + Modal Close Chrome

## Phone finding from v1.4.44

Real-phone landscape screenshots confirmed that UX-021's responsive row is active, but
the first build is not accepted:

- Recent-file / backup chooser footer placed three actions in one row;
- `Системний вибір файла…` wrapped/clipped inside the fixed-height button;
- Storage save footer itself fit acceptably;
- modal `Закрити` was rendered as colored text without the same button background as
  the other actions.

Therefore v1.4.44 is a **PHONE FAIL** for the targeted visual acceptance.

## R1 correction

- when the Recent-file footer is using the horizontal action row, use the explicit
  compact label `Системний вибір…`;
- portrait / stacked layout keeps `Системний вибір файла…`;
- dismissive modal actions such as `Закрити` use the same boxed action chrome as the
  other dialog buttons;
- remove the obsolete transparent text-only Close helper.

## Version

- versionName: **1.4.44-R1**
- versionCode: **84**

## Phone acceptance

1. Recent-file / backup chooser in landscape:
   - three footer actions stay in one row;
   - `Системний вибір…` is readable and not vertically clipped.
2. Storage save footer:
   - `Додати папку… / Зберегти як… / Скасувати` remain readable.
3. Result/problem modal:
   - `Закрити` has a normal button background matching the action area.
4. Rotate back to portrait:
   - no action fires;
   - navigation/state remains usable.

Status: **IMPLEMENTED / PHONE RETEST NEEDED**.
