# YTM Importer v1.4.38-R2

- versionName: **1.4.38-R2**
- versionCode: **74**
- status: **NOT PHONE-TESTED YET**
- scope: **checkbox visual centering only**

## R1 phone result

- Restore confirmation rotation persistence: **PASS**.
- Multi-select checkbox alignment: **FAIL**.

The R1 checkbox lived in a 48dp touch view, but Android's `CheckBox` button drawable still rendered toward the start side of that view. The touch column itself was centered; the visible square was not.

## R2 correction

R2 keeps the same 48dp touch column but wraps the `CheckBox` in a `FrameLayout`.

The actual checkbox view is added with:

- `WRAP_CONTENT × WRAP_CONTENT`;
- `Gravity.CENTER`.

This centers the visible checkbox drawable inside the fixed column while preserving:

- the 48dp touch zone;
- whole-row tapping;
- the existing text column;
- multi-select state and counter behavior.

## Phone acceptance

R2 needs one visual phone check:

- in `Плейлисти для експорту`, the visible checkbox square should no longer hug the left card edge and should look balanced between the card edge and playlist text.

BUG-008 Restore rotation is already accepted from R1 and does not need another functional retest unless the Data flow is otherwise changed.
