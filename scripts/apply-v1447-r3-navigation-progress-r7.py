#!/usr/bin/env python3
from __future__ import annotations
import argparse
from pathlib import Path

BASE_SHA = "ee25044a152ba867aa6840fbf2a5a776d1c112b9"
OPS = [{'file': 'app/src/main/java/com/saney/ytmimporter/MainActivity.kt', 'name': 'Track active write lifecycle in Main', 'old': '    private lateinit var workflowRelay: WorkflowRelayOverlay\n\n    private val uiPrefs by lazy {\n', 'new': '    private lateinit var workflowRelay: WorkflowRelayOverlay\n    private var writeInProgress = false\n\n    private val uiPrefs by lazy {\n'}, {'file': 'app/src/main/java/com/saney/ytmimporter/MainActivity.kt', 'name': 'Do not reload stale workspace during active write', 'old': '        if (::adapter.isInitialized) {\n            reloadCurrentWorkspace()\n        }\n', 'new': '        if (\n            ::adapter.isInitialized &&\n            !writeInProgress\n        ) {\n            reloadCurrentWorkspace()\n        }\n'}, {'file': 'app/src/main/java/com/saney/ytmimporter/MainActivity.kt', 'name': 'Home step 3 uses direct navigation ownership', 'old': '                ) {\n                    searchOrReview()\n                }.apply {\n', 'new': '                ) {\n                    openDirectSearchOrReview()\n                }.apply {\n'}, {'file': 'app/src/main/java/com/saney/ytmimporter/MainActivity.kt', 'name': 'Home step 4 uses direct navigation ownership', 'old': '                ) {\n                    createPlaylist()\n                }.apply {\n', 'new': '                ) {\n                    openDirectDestination()\n                }.apply {\n'}, {'file': 'app/src/main/java/com/saney/ytmimporter/MainActivity.kt', 'name': 'Home current-playlist card clears stale delegated ownership', 'old': '                subtitleAccent = true,\n                onClick = {\n                    openPlaylistHub()\n                }\n', 'new': '                subtitleAccent = true,\n                onClick = {\n                    openDirectPlaylistHub()\n                }\n'}, {'file': 'app/src/main/java/com/saney/ytmimporter/MainActivity.kt', 'name': 'Bottom navigation owns direct search and playlist roots', 'old': '                    onSearch = {\n                        searchOrReview()\n                    },\n                    onPlaylist = {\n                        openPlaylistHub()\n                    },\n', 'new': '                    onSearch = {\n                        openDirectSearchOrReview()\n                    },\n                    onPlaylist = {\n                        openDirectPlaylistHub()\n                    },\n'}, {'file': 'app/src/main/java/com/saney/ytmimporter/MainActivity.kt', 'name': 'Add explicit direct/delegated route boundary helpers', 'old': '    private fun openPlaylistHub() {\n        startActivityForResult(\n', 'new': '    private fun clearDelegatedReturnRoute() {\n        returnToPlaylistHubAfterDelegatedAction = false\n        returnToMenuAfterDelegatedAction = false\n\n        if (::workflowRelay.isInitialized) {\n            hideWorkflowRelayOverlay()\n        }\n    }\n\n    private fun openDirectSearchOrReview() {\n        clearDelegatedReturnRoute()\n        searchOrReview()\n    }\n\n    private fun openDirectDestination() {\n        clearDelegatedReturnRoute()\n        createPlaylist()\n    }\n\n    private fun openDirectPlaylistHub() {\n        clearDelegatedReturnRoute()\n        openPlaylistHub()\n    }\n\n    private fun openPlaylistHub() {\n        startActivityForResult(\n'}, {'file': 'app/src/main/java/com/saney/ytmimporter/MainActivity.kt', 'name': 'Capture stable live display track list for write', 'old': '    ) {\n        workflowRelay.showWriteProgress(initialJob.playlistName, playlist?.tracks ?: tracks, statusText.text.toString())\n\n        executor.execute {\n', 'new': '    ) {\n        writeInProgress = true\n\n        val displayTracks =\n            playlist?.tracks\n                ?: tracks\n\n        workflowRelay.showWriteProgress(\n            initialJob.playlistName,\n            displayTracks,\n            statusText.text.toString()\n        )\n\n        executor.execute {\n'}, {'file': 'app/src/main/java/com/saney/ytmimporter/MainActivity.kt', 'name': 'Use stable write rows for active track', 'old': '                        runOnUiThread {\n                            workflowRelay.updateWriteTracks(playlist?.tracks ?: tracks, track)\n                        }\n', 'new': '                        runOnUiThread {\n                            workflowRelay.updateWriteTracks(\n                                displayTracks,\n                                track\n                            )\n                        }\n'}, {'file': 'app/src/main/java/com/saney/ytmimporter/MainActivity.kt', 'name': 'Use stable write rows for progress', 'old': '                            workflowRelay.updateWriteProgress(writeProgress, playlist?.tracks ?: tracks)\n\n                            adapter.notifyDataSetChanged()\n', 'new': '                            workflowRelay.updateWriteProgress(\n                                writeProgress,\n                                displayTracks\n                            )\n\n                            adapter.notifyDataSetChanged()\n'}, {'file': 'app/src/main/java/com/saney/ytmimporter/MainActivity.kt', 'name': 'Close write lifecycle before terminal UI and persist states', 'old': '            runOnUiThread {\n                progress.visibility = View.GONE\n                adapter.notifyDataSetChanged()\n', 'new': '            runOnUiThread {\n                writeInProgress = false\n                persistCurrentWorkspace()\n\n                progress.visibility = View.GONE\n                adapter.notifyDataSetChanged()\n'}, {'file': 'app/src/main/java/com/saney/ytmimporter/ui/WorkflowRelayOverlay.kt', 'name': 'Truthful write aggregate progress', 'old': '        val nextMessage =\n            if (\n                progress.playlistCreated\n            ) {\n                "Плейлист створено. Додаю треки…"\n            } else {\n                "Додано ${progress.processedTracks}/${progress.totalTracks}… " +\n                    "залишилось ${progress.job.remainingTracks.size}"\n            }\n', 'new': '        val skippedDuplicates =\n            tracks.count {\n                it.status ==\n                    TrackStatus.DUPLICATE\n            }\n\n        val nextMessage =\n            if (\n                progress.playlistCreated\n            ) {\n                "Плейлист створено. Додаю треки…"\n            } else {\n                "Оброблено ${progress.processedTracks}/${progress.totalTracks} • " +\n                    "залишилось ${progress.job.remainingTracks.size}\\n" +\n                    "Додано ${progress.job.addedCount} • " +\n                    "дублікатів пропущено $skippedDuplicates • " +\n                    "помилок ${progress.job.failedCount}"\n            }\n'}, {'file': 'app/src/main/java/com/saney/ytmimporter/ui/WorkflowRelayOverlay.kt', 'name': 'Semantic prefixes for terminal write rows', 'old': '                        when {\n                            isActive ->\n                                "● "\n\n                            track.status ==\n                                TrackStatus.ADDED ->\n                                "✓ "\n\n                            else ->\n                                ""\n                        }\n', 'new': '                        when {\n                            isActive ->\n                                "● "\n\n                            track.status ==\n                                TrackStatus.ADDED ->\n                                "✓ "\n\n                            track.status ==\n                                TrackStatus.DUPLICATE ->\n                                "≋ "\n\n                            track.status ==\n                                TrackStatus.FAILED ->\n                                "× "\n\n                            track.status ==\n                                TrackStatus.SKIPPED ->\n                                "— "\n\n                            else ->\n                                ""\n                        }\n'}, {'file': 'app/src/main/java/com/saney/ytmimporter/ui/WorkflowRelayOverlay.kt', 'name': 'Semantic title colors for write rows', 'old': '                        when {\n                            isActive ->\n                                palette.accent\n\n                            track.status ==\n                                TrackStatus.ADDED ->\n                                palette.success\n\n                            else ->\n                                palette.text\n                        }\n', 'new': '                        when {\n                            isActive ->\n                                palette.accent\n\n                            track.status ==\n                                TrackStatus.ADDED ->\n                                palette.success\n\n                            track.status ==\n                                TrackStatus.DUPLICATE ->\n                                palette.duplicate\n\n                            track.status ==\n                                TrackStatus.FAILED ->\n                                palette.danger\n\n                            track.status ==\n                                TrackStatus.SKIPPED ->\n                                palette.muted\n\n                            else ->\n                                palette.text\n                        }\n'}, {'file': 'app/src/main/java/com/saney/ytmimporter/ReviewActivity.kt', 'name': 'Do not clear repeat-search state for configuration cancel', 'old': '                    dialog.setOnCancelListener {\n                        repeatSearchDialogOpen = false\n                        repeatSearchDialog = null\n                    }\n', 'new': '                    dialog.setOnCancelListener {\n                        if (!isChangingConfigurations) {\n                            repeatSearchDialogOpen = false\n                            repeatSearchDialog = null\n                        }\n                    }\n'}, {'file': 'app/src/main/AndroidManifest.xml', 'name': 'Keep Review activity and active dialogs alive across rotation', 'old': '        <activity\n            android:name=".ReviewActivity"\n            android:exported="false"\n            android:screenOrientation="unspecified" />\n', 'new': '        <activity\n            android:name=".ReviewActivity"\n            android:configChanges="keyboardHidden|orientation|screenSize|smallestScreenSize"\n            android:exported="false"\n            android:screenOrientation="unspecified" />\n'}, {'file': 'scripts/release-preflight.sh', 'name': 'Release preflight registers R7 audit file', 'old': 'check_file "scripts/v1447-r3-runtime-lifecycle-r5-audit.sh"\ncheck_file "scripts/v1447-r3-flow-result-lifecycle-r6-audit.sh"\n\nbash scripts/mainactivity-audit.sh\n', 'new': 'check_file "scripts/v1447-r3-runtime-lifecycle-r5-audit.sh"\ncheck_file "scripts/v1447-r3-flow-result-lifecycle-r6-audit.sh"\ncheck_file "scripts/v1447-r3-navigation-progress-r7-audit.sh"\n\nbash scripts/mainactivity-audit.sh\n'}, {'file': 'scripts/release-preflight.sh', 'name': 'Release preflight executes R7 audit', 'old': 'bash scripts/v1447-r3-runtime-lifecycle-r5-audit.sh\nbash scripts/v1447-r3-flow-result-lifecycle-r6-audit.sh\nbash scripts/project-handoff-audit.sh\n', 'new': 'bash scripts/v1447-r3-runtime-lifecycle-r5-audit.sh\nbash scripts/v1447-r3-flow-result-lifecycle-r6-audit.sh\nbash scripts/v1447-r3-navigation-progress-r7-audit.sh\nbash scripts/project-handoff-audit.sh\n'}]
NEW_FILES = {'docs/v.1.4.47/navigation/TEST_DIAGRAM_STANDARD.md': "# YTM Importer — стандарт тестових діаграм\n\nЦей документ є обов'язковим контрактом для майбутніх phone-QA діаграм.\n\n## 1. Повний маршрут\n\nКожна тестова діаграма **завжди починається з `[Головна]`**.\n\nПорядок:\n1. `[Головна]`;\n2. конкретний блок головної сторінки;\n3. конкретний об'єкт/кнопка блока;\n4. кожний наступний екран, вікно або системний крок;\n5. ціль тесту;\n6. фінальний результат `PASS` / `FAIL`.\n\nЛокальні фрагменти, що починаються одразу з внутрішнього екрана або progress-вікна, не є повною тестовою діаграмою.\n\n## 2. Об'єкти на кожному кроці\n\nДля КОЖНОГО кроку маршруту окремими гілками показуються всі релевантні об'єкти:\n- кнопки;\n- поля вводу;\n- фільтри;\n- інформаційні блоки;\n- Help windows / довідкові вікна;\n- вікна підтвердження;\n- progress/status вікна;\n- result/error вікна;\n- `←`;\n- Android system Back;\n- `Скасувати`;\n- `Закрити`;\n- дії підтвердження;\n- rotation / recreation, якщо це частина тесту.\n\n## 3. Стани\n\nНа кожному кроці показуються всі стани, які можуть змінити поведінку або вигляд:\n- loading / завантаження;\n- empty / порожньо;\n- enabled / disabled;\n- pending / очікує;\n- active / виконується;\n- added / додано;\n- duplicate / дублікат;\n- failed / помилка;\n- completed / завершено;\n- authorization / quota стани, якщо вони належать маршруту.\n\nСтани запису в YTM у UI позначаються насамперед українською:\n- `… Очікує`;\n- `● Додаю…`;\n- `✓ Додано`;\n- `≋ Дублікат • пропущено`;\n- `× Помилка`.\n\n## 4. Мова\n\nПервинна мова діаграм — **українська**.\n\nНазви екранів, кнопок, полів і вікон мають повторювати фактичний український UI застосунку.\n\nАнглійська термінологія дозволена тільки:\n- як вторинне уточнення в дужках;\n- або якщо англійський термін уже присутній у самому UI.\n\nПриклад: `кеш (cache)`, а не `cache` як основний підпис.\n\n## 5. Джерело істини\n\nМаршрути не вгадуються.\n\nПеред побудовою діаграми маршрут і всі доступні дії перевіряються за актуальним source code тієї самої commit-версії, яку тестує APK.\n\nPhone-QA є остаточним підтвердженням фактичної поведінки.\n\n## 6. Власник маршруту\n\nОднаковий внутрішній екран може бути відкритий з різних точок входу. Ці маршрути **не об'єднуються**, якщо Back/Cancel повинен повертати до різних батьків.\n\nПриклади:\n- `[Головна] → 4 кроки до плейлиста → 3. Знайти / перевірити → [Перевірка треків]`\n  має власника `Головна`; Back повертає на `[Головна]`.\n- `[Головна] → Поточний плейлист → [Поточний плейлист] → Знайти / перевірити → [Перевірка треків]`\n  має власника `Поточний плейлист`; Back повертає в `[Поточний плейлист]`.\n- `[Головна] → нижня панель → Плейлист → [Поточний плейлист] → ...`\n  також тестується як окрема точка входу, навіть якщо використовує той самий `PlaylistActivity`.\n\nДіаграма завжди показує не тільки `куди зайшли`, а й `кому належить Back`.\n\n## 7. Пакети тестів\n\nPhone-QA подається користувачу максимум по **3 тести за один пакет**.\n\nКожний тест має власну повну діаграму.\n\n## 8. MOBILE / DESKTOP структура\n\nРекомендована структура:\n\n```text\n<test-package>/\n├── index.html\n├── README.md\n├── mobile/\n│   ├── TEST_01.svg\n│   └── TEST_01.png\n└── desktop/\n    ├── TEST_01.svg\n    └── TEST_01.png\n```\n\n- `index.html` знаходиться на рівень вище `mobile/` і `desktop/`;\n- MOBILE: зверху вниз, орієнтація під телефон;\n- DESKTOP: зліва направо, орієнтація під ПК;\n- логіка, ID вузлів, стани й підписи в обох варіантах однакові; змінюється тільки layout.\n\n## 9. Візуальні типи вузлів\n\nРекомендована семантика:\n- `[Екран]` — екран / Activity;\n- `{Вікно}` — modal / confirm / result;\n- `{Help}` — інформаційне або довідкове вікно;\n- `[/ Поле /]` — поле вводу / пошуку / вибору;\n- `Стан:` — runtime state;\n- `PASS` / `FAIL` — кінцева перевірка.\n\nОсновний тестовий шлях виділяється сильніше; альтернативні можливості — боковими гілками.\n\n## 10. Phone-QA звіт\n\nКористувач може відповідати коротко:\n`1+`, `2-`, `3+`.\n\nЯкщо FAIL стосується конкретного вузла, він описується через шлях/стан, наприклад:\n`WRITE.TRACK.ADDED → FAIL: UI залишився «… Очікує»`.\n\nЦе дозволяє прив'язати finding до конкретного transition/state у source code.\n", 'docs/v.1.4.47/navigation/NAVIGATION_ORIGIN_CONTRACT_R7.md': '# R7 — контракт власника навігаційного маршруту\n\nМета: не змішувати прямі Home-маршрути з delegated-маршрутами через `Поточний плейлист` або `Меню`.\n\n## Прямий Home route\n\n### Home block `4 кроки до плейлиста` → `3. Знайти / перевірити`\n\n```text\n[Головна]\n→ 4 кроки до плейлиста\n→ 3. Знайти / перевірити\n→ [Перевірка треків]\n```\n\n`←` та Android system Back із `[Перевірка треків]`:\n**повернення на `[Головна]`**.\n\nЦей маршрут не має права успадковувати старий `returnToPlaylistHubAfterDelegatedAction`.\n\n### Home block `4 кроки до плейлиста` → `4. Створити / додати`\n\nЦе також прямий Home route. Back із Destination flow повертає до прямого Home/Review контексту, а не самовільно відкриває `Поточний плейлист`.\n\n## Playlist-owned route\n\n### Home block `Поточний плейлист`\n\n```text\n[Головна]\n→ Поточний плейлист\n→ [Поточний плейлист]\n→ Знайти / перевірити\n→ [Перевірка треків]\n```\n\nЯкщо Review був відкритий як delegated action із `[Поточний плейлист]`,\nBack повертає у `[Поточний плейлист]`.\n\n### Нижня панель → `Плейлист`\n\n```text\n[Головна]\n→ нижня панель\n→ Плейлист\n→ [Поточний плейлист]\n→ ...\n```\n\nЦе окрема точка входу для діаграми, хоча вона використовує той самий Playlist screen і той самий delegated parent contract.\n\n## Нижня панель → `Пошук`\n\nЦе прямий search/review route:\nBack із `[Перевірка треків]` повертає на `[Головна]`, якщо користувач не зайшов у Review через Playlist-owned flow.\n\n## Технічний контракт\n\n- direct Home/bottom-search entry очищає delegated return flags перед відкриттям Review/Destination;\n- direct Playlist entry очищає старі delegated flags перед відкриттям Playlist root;\n- тільки `handlePlaylistHubResult(...)` має встановлювати Playlist delegated return ownership через `beginPlaylistRelay(...)`;\n- тільки Menu delegated actions мають встановлювати Menu return ownership через `beginMenuRelay(...)`;\n- Back/Cancel не повинен перекидати користувача в іншу гілку Home, ніж та, з якої почався маршрут.\n', 'docs/v.1.4.47/qa/R3_NAV_PROGRESS_LIFECYCLE_R7.md': '# v1.4.47-R3 — Navigation / Progress / Lifecycle R7\n\nBase:\n- branch: `fix/v1.4.47-r3-bugfix-wave`\n- expected base commit: `ee25044a152ba867aa6840fbf2a5a776d1c112b9`\n\nPhone findings before R7:\n1. Home block 2 → `3. Знайти / перевірити` reaches Review/Destination, but Back ownership can collapse into the Playlist-owned path. Direct Home and Playlist-owned routes must remain separate.\n2. Write flow itself starts, but live progress rows can remain visually stale (`… Очікує`) and active/success colors do not reliably represent the coordinator state.\n3. Aggregate progress text `Додано X/Y` is semantically wrong when `X` is processed tracks rather than actual successful inserts.\n4. `Повторити пошук?` still disappears on rotation.\n5. Approved QA diagram format is full object-map from `[Головна]`, with all objects/states at every step, Ukrainian-first labels, separate MOBILE/DESKTOP folders, and `index.html` above them.\n\nR7 change contract:\n- separate direct Home/bottom-search navigation ownership from Playlist/Menu delegated return ownership;\n- prevent `onResume()` from replacing live Track objects while a write is running;\n- keep one stable display-track list for the entire active write;\n- progress aggregate uses `Оброблено`, actual `Додано`, skipped duplicates, errors and remaining count;\n- semantic row title colors/prefixes are driven by actual state;\n- Review handles orientation changes without destroying its active confirmation dialog;\n- repeat-search cancel listener does not clear lifecycle state during configuration changes;\n- persist the approved test-diagram standard in the repository.\n\nPhone acceptance after signed build:\n1. `[Головна] → 3. Знайти / перевірити → [Перевірка треків] → Back` returns to `[Головна]`, never to `Поточний плейлист`.\n2. Playlist-owned path still returns to `[Поточний плейлист]`.\n3. Real write shows live `● Додаю…`, then `✓ Додано` / duplicate / error with correct semantic colors and truthful aggregate text.\n4. `Перевірка треків → ↻ Пошук → Повторити пошук?` survives horizontal and vertical rotation.\n', 'scripts/v1447-r3-navigation-progress-r7-audit.sh': '#!/usr/bin/env bash\nset -euo pipefail\nfail(){ echo "FAIL: $1" >&2; exit 1; }\n\nMAIN="app/src/main/java/com/saney/ytmimporter/MainActivity.kt"\nREVIEW="app/src/main/java/com/saney/ytmimporter/ReviewActivity.kt"\nRELAY="app/src/main/java/com/saney/ytmimporter/ui/WorkflowRelayOverlay.kt"\nMANIFEST="app/src/main/AndroidManifest.xml"\nSTD="docs/v.1.4.47/navigation/TEST_DIAGRAM_STANDARD.md"\nOWN="docs/v.1.4.47/navigation/NAVIGATION_ORIGIN_CONTRACT_R7.md"\n\nfor f in "$MAIN" "$REVIEW" "$RELAY" "$MANIFEST" "$STD" "$OWN"; do\n  test -f "$f" || fail "missing R7 file: $f"\ndone\n\npython - "$MAIN" "$REVIEW" "$RELAY" "$MANIFEST" "$STD" "$OWN" <<\'PY\'\nfrom pathlib import Path\nimport sys\n\nmain, review, relay, manifest, standard, ownership = [\n    Path(p).read_text(encoding="utf-8")\n    for p in sys.argv[1:]\n]\n\nfor needle in [\n    "private var writeInProgress = false",\n    "!writeInProgress",\n    "private fun clearDelegatedReturnRoute()",\n    "private fun openDirectSearchOrReview()",\n    "private fun openDirectDestination()",\n    "private fun openDirectPlaylistHub()",\n]:\n    if needle not in main:\n        raise SystemExit(f"FAIL: direct-route/write lifecycle contract missing: {needle}")\n\nplaylist_handler = main[\n    main.index("private fun handlePlaylistHubResult("):\n    main.index("private fun handleMenuScreenResult(")\n]\nif \'beginPlaylistRelay("Поточний плейлист"\' not in playlist_handler:\n    raise SystemExit("FAIL: Playlist-owned search no longer sets Playlist return ownership")\nif \'beginPlaylistRelay("Створити / додати"\' not in playlist_handler:\n    raise SystemExit("FAIL: Playlist-owned destination no longer sets Playlist return ownership")\n\nwrite_start = main.index("private fun executeWriteJob(")\nwrite = main[write_start:main.index("private fun showQuotaPausedDialog(", write_start)]\nfor needle in [\n    "writeInProgress = true",\n    "val displayTracks =",\n    "workflowRelay.updateWriteTracks(",\n    "workflowRelay.updateWriteProgress(",\n    "writeInProgress = false",\n    "persistCurrentWorkspace()",\n]:\n    if needle not in write:\n        raise SystemExit(f"FAIL: stable write UI contract missing: {needle}")\n\nfor needle in [\n    \'"Оброблено ${progress.processedTracks}/${progress.totalTracks} • "\',\n    \'"Додано ${progress.job.addedCount} • "\',\n    "TrackStatus.DUPLICATE",\n    "palette.duplicate",\n    "TrackStatus.FAILED",\n    "palette.danger",\n]:\n    if needle not in relay:\n        raise SystemExit(f"FAIL: progress semantic state missing: {needle}")\n\nrepeat_start = review.index("private fun showRepeatSearchDialog()")\nrepeat = review[repeat_start:review.index("private fun reloadSnapshot()", repeat_start)]\nif "if (!isChangingConfigurations)" not in repeat:\n    raise SystemExit("FAIL: repeat-search config cancel guard missing")\n\nreview_pos = manifest.index(\'android:name=".ReviewActivity"\')\nreview_manifest = manifest[review_pos - 40:manifest.index(\'android:name=".ImportActivity"\')]\nif \'android:configChanges="keyboardHidden|orientation|screenSize|smallestScreenSize"\' not in review_manifest:\n    raise SystemExit("FAIL: Review rotation preservation config missing")\n\nfor needle in [\n    "завжди починається з `[Головна]`",\n    "Первинна мова діаграм — **українська**",\n    "MOBILE / DESKTOP",\n    "Власник маршруту",\n]:\n    if needle not in standard:\n        raise SystemExit(f"FAIL: diagram standard missing: {needle}")\n\nfor needle in [\n    "3. Знайти / перевірити",\n    "повернення на `[Головна]`",\n    "Поточний плейлист",\n    "нижня панель",\n]:\n    if needle not in ownership:\n        raise SystemExit(f"FAIL: navigation origin doc missing: {needle}")\n\nif len(main.splitlines()) >= 4100:\n    raise SystemExit("FAIL: MainActivity unexpectedly exceeded 4100 lines")\nPY\n\necho "PASS:"\necho "- direct Home search/destination routes are separated from Playlist/Menu delegated routes"\necho "- Playlist-owned route still preserves Playlist return ownership"\necho "- onResume cannot replace live write Track objects"\necho "- write overlay uses stable track references and truthful aggregate semantics"\necho "- terminal row prefixes/colors are semantic"\necho "- repeat-search rotation contract is protected"\necho "- approved full object-map diagram standard is persisted"\n'}

def patch_one(op, do_apply):
    path = Path(op["file"])
    if not path.exists():
        raise SystemExit(f"FAIL: missing file: {path}")
    text = path.read_text(encoding="utf-8")
    old, new = op["old"], op["new"]
    old_count = text.count(old)
    new_count = text.count(new)
    if new_count == 1 and old_count == 0:
        print(f"SKIP: already applied: {op['name']}")
        return
    if new_count == 1 and old_count == 1 and len(new) > len(old):
        print(f"SKIP: already applied: {op['name']}")
        return
    if old_count != 1 or new_count != 0:
        raise SystemExit(
            f"FAIL: R7 anchor mismatch for {op['name']}: old={old_count}, new={new_count}"
        )
    print(f"READY: {op['name']}")
    if do_apply:
        path.write_text(text.replace(old, new, 1), encoding="utf-8", newline="\n")
        print(f"APPLIED: {op['name']}")

def ensure_file(rel, content, do_apply):
    path = Path(rel)
    if path.exists():
        if path.read_text(encoding="utf-8") == content:
            print(f"SKIP: already present: {rel}")
            return
        raise SystemExit(f"FAIL: generated file differs: {rel}")
    print(f"READY: create {rel}")
    if do_apply:
        path.parent.mkdir(parents=True, exist_ok=True)
        path.write_text(content, encoding="utf-8", newline="\n")
        print(f"CREATED: {rel}")

def main():
    parser = argparse.ArgumentParser()
    parser.add_argument("--check", action="store_true")
    args = parser.parse_args()
    for op in OPS:
        patch_one(op, not args.check)
    for rel, content in NEW_FILES.items():
        ensure_file(rel, content, not args.check)
    print("PASS: R7 ready/already applied" if args.check else "PASS: R7 applied")

if __name__ == "__main__":
    main()
