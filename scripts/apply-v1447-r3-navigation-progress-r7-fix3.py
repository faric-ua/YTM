#!/usr/bin/env python3
from pathlib import Path
import sys

OPS = [{'file': 'scripts/mainactivity-audit.sh', 'old': 'LINES="$(wc -l < "$MAIN" | tr -d \' \')"\n\necho "YTM Importer MainActivity audit"\necho "Lines: $LINES"\n', 'new': 'MAIN_LINES="$(wc -l < "$MAIN" | tr -d \' \')"\n\necho "YTM Importer MainActivity audit"\necho "Lines: $MAIN_LINES"\n', 'name': 'Use non-special MAIN_LINES in mainactivity audit'}, {'file': 'scripts/mainactivity-cleanup-audit.sh', 'old': 'LINES="$(wc -l < "$MAIN" | tr -d \' \')"\n\n# v1.4.47-R3 R7 adds explicit navigation-origin ownership and active-write\n# lifecycle guards. The old <4000 gate predates those successor contracts.\n# Keep a hard cap, aligned with the R7 audit, without weakening the\n# architectural checks below.\n[ "$LINES" -lt 4100 ] \\\n  || fail "MainActivity exceeded the R7 successor budget: $LINES lines"\n', 'new': 'MAIN_LINES="$(wc -l < "$MAIN" | tr -d \' \')"\n\n# v1.4.47-R3 R7 adds explicit navigation-origin ownership and active-write\n# lifecycle guards. The old <4000 gate predates those successor contracts.\n# Keep a hard cap, aligned with the R7 audit, without weakening the\n# architectural checks below.\n[ "$MAIN_LINES" -lt 4100 ] \\\n  || fail "MainActivity exceeded the R7 successor budget: $MAIN_LINES lines"\n', 'name': 'Use non-special MAIN_LINES in cleanup budget'}, {'file': 'scripts/mainactivity-cleanup-audit.sh', 'old': 'echo "- MainActivity reduced to $LINES lines"\n', 'new': 'echo "- MainActivity reduced to $MAIN_LINES lines"\n', 'name': 'Report real MainActivity lines in cleanup audit'}, {'file': 'scripts/v1447-r1-audit.sh', 'old': 'LINES="$(wc -l < "$MAIN" | tr -d \' \')"\nLIMIT=4000\nif grep -Fq \'private var writeInProgress = false\' "$MAIN"; then\n  LIMIT=4100\nfi\n[ "$LINES" -lt "$LIMIT" ] ||\n  fail "MainActivity cleanup regression: $LINES lines (limit <$LIMIT)"\n', 'new': 'MAIN_LINES="$(wc -l < "$MAIN" | tr -d \' \')"\nLIMIT=4000\nif grep -Fq \'private var writeInProgress = false\' "$MAIN"; then\n  LIMIT=4100\nfi\n[ "$MAIN_LINES" -lt "$LIMIT" ] ||\n  fail "MainActivity cleanup regression: $MAIN_LINES lines (limit <$LIMIT)"\n', 'name': 'R1 uses reliable MainActivity line variable'}, {'file': 'scripts/v1447-r1-audit.sh', 'old': 'echo "- MainActivity $LINES lines"\n', 'new': 'echo "- MainActivity $MAIN_LINES lines"\n', 'name': 'R1 reports real MainActivity line count'}, {'file': 'scripts/v1447-r2-audit.sh', 'old': 'LINES="$(wc -l < "$MAIN" | tr -d \' \')"\nLIMIT=4000\nif grep -Fq \'private var writeInProgress = false\' "$MAIN"; then\n  LIMIT=4100\nfi\n[ "$LINES" -lt "$LIMIT" ] ||\n  fail "MainActivity cleanup regression: $LINES lines (limit <$LIMIT)"\n', 'new': 'MAIN_LINES="$(wc -l < "$MAIN" | tr -d \' \')"\nLIMIT=4000\nif grep -Fq \'private var writeInProgress = false\' "$MAIN"; then\n  LIMIT=4100\nfi\n[ "$MAIN_LINES" -lt "$LIMIT" ] ||\n  fail "MainActivity cleanup regression: $MAIN_LINES lines (limit <$LIMIT)"\n', 'name': 'R2 uses reliable MainActivity line variable'}, {'file': 'scripts/v1447-r2-audit.sh', 'old': 'echo "- MainActivity $LINES lines"\n', 'new': 'echo "- MainActivity $MAIN_LINES lines"\n', 'name': 'R2 reports real MainActivity line count'}, {'file': 'scripts/v1447-r3-corrective-navigation-audit.sh', 'old': 'LINES="$(wc -l < "$MAIN" | tr -d \' \')"\nLIMIT=4000\nif grep -Fq \'private var writeInProgress = false\' "$MAIN"; then\n  LIMIT=4100\nfi\n[ "$LINES" -lt "$LIMIT" ] ||\n  fail "MainActivity corrective bridge exceeds cleanup ceiling: $LINES (limit <$LIMIT)"\n', 'new': 'MAIN_LINES="$(wc -l < "$MAIN" | tr -d \' \')"\nLIMIT=4000\nif grep -Fq \'private var writeInProgress = false\' "$MAIN"; then\n  LIMIT=4100\nfi\n[ "$MAIN_LINES" -lt "$LIMIT" ] ||\n  fail "MainActivity corrective bridge exceeds cleanup ceiling: $MAIN_LINES (limit <$LIMIT)"\n', 'name': 'Corrective navigation uses reliable line variable'}, {'file': 'scripts/v1447-r3-corrective-navigation-audit.sh', 'old': 'echo "- MainActivity stays below active cleanup ceiling (<$LIMIT; current $LINES)"\n', 'new': 'echo "- MainActivity stays below active cleanup ceiling (<$LIMIT; current $MAIN_LINES)"\n', 'name': 'Corrective navigation reports real line count'}, {'file': 'scripts/v1447-r3-corrective-navigation-fix3-audit.sh', 'old': 'LINES="$(wc -l < "$MAIN" | tr -d \' \')"\nLIMIT=4000\nif grep -Fq \'private var writeInProgress = false\' "$MAIN"; then\n  LIMIT=4100\nfi\n[ "$LINES" -lt "$LIMIT" ] ||\n  fail "MainActivity exceeds active cleanup ceiling: $LINES (limit <$LIMIT)"\n', 'new': 'MAIN_LINES="$(wc -l < "$MAIN" | tr -d \' \')"\nLIMIT=4000\nif grep -Fq \'private var writeInProgress = false\' "$MAIN"; then\n  LIMIT=4100\nfi\n[ "$MAIN_LINES" -lt "$LIMIT" ] ||\n  fail "MainActivity exceeds active cleanup ceiling: $MAIN_LINES (limit <$LIMIT)"\n', 'name': 'Corrective FIX3 uses reliable line variable'}, {'file': 'scripts/v1447-r3-corrective-navigation-fix3-audit.sh', 'old': 'echo "- MainActivity remains below active cleanup ceiling (<$LIMIT; current $LINES)"\n', 'new': 'echo "- MainActivity remains below active cleanup ceiling (<$LIMIT; current $MAIN_LINES)"\n', 'name': 'Corrective FIX3 reports real line count'}, {'file': 'scripts/v1447-r3-corrective-navigation-fix6-audit.sh', 'old': 'LINES="$(wc -l < "$MAIN" | tr -d \' \')"\nLIMIT=4000\nif grep -Fq \'private var writeInProgress = false\' "$MAIN"; then\n  LIMIT=4100\nfi\n[ "$LINES" -lt "$LIMIT" ] ||\n  fail "MainActivity exceeds active cleanup ceiling: $LINES (limit <$LIMIT)"\n', 'new': 'MAIN_LINES="$(wc -l < "$MAIN" | tr -d \' \')"\nLIMIT=4000\nif grep -Fq \'private var writeInProgress = false\' "$MAIN"; then\n  LIMIT=4100\nfi\n[ "$MAIN_LINES" -lt "$LIMIT" ] ||\n  fail "MainActivity exceeds active cleanup ceiling: $MAIN_LINES (limit <$LIMIT)"\n', 'name': 'Corrective FIX6 uses reliable line variable'}, {'file': 'scripts/v1447-r3-corrective-navigation-fix6-audit.sh', 'old': 'echo "- MainActivity remains below active cleanup ceiling (<$LIMIT; current $LINES)"\n', 'new': 'echo "- MainActivity remains below active cleanup ceiling (<$LIMIT; current $MAIN_LINES)"\n', 'name': 'Corrective FIX6 reports real line count'}, {'file': 'scripts/v1447-r3-navigation-ownership-audit.sh', 'old': 'LINES="$(wc -l < "$MAIN" | tr -d \' \')"\nLIMIT=4000\nif grep -Fq \'private var writeInProgress = false\' "$MAIN"; then\n  LIMIT=4100\nfi\n[ "$LINES" -lt "$LIMIT" ] ||\n  fail "MainActivity cleanup regression: $LINES lines (limit <$LIMIT)"\n', 'new': 'MAIN_LINES="$(wc -l < "$MAIN" | tr -d \' \')"\nLIMIT=4000\nif grep -Fq \'private var writeInProgress = false\' "$MAIN"; then\n  LIMIT=4100\nfi\n[ "$MAIN_LINES" -lt "$LIMIT" ] ||\n  fail "MainActivity cleanup regression: $MAIN_LINES lines (limit <$LIMIT)"\n', 'name': 'Navigation ownership uses reliable line variable'}, {'file': 'scripts/v1447-r3-navigation-ownership-audit.sh', 'old': 'echo "- MainActivity remains below active cleanup ceiling (<$LIMIT; current $LINES)"\n', 'new': 'echo "- MainActivity remains below active cleanup ceiling (<$LIMIT; current $MAIN_LINES)"\n', 'name': 'Navigation ownership reports real line count'}, {'file': 'docs/v.1.4.47/qa/R3_NAV_PROGRESS_LIFECYCLE_R7.md', 'old': '- align historical MainActivity line-budget checks with an explicit successor rule: historical source keeps `<4000`; R7 source identified by the active-write lifecycle marker uses `<4100`. All legacy-flow and dedicated-activity ownership checks remain unchanged.\n\nPhone acceptance after signed build:\n', 'new': "- align historical MainActivity line-budget checks with an explicit successor rule: historical source keeps `<4000`; R7 source identified by the active-write lifecycle marker uses `<4100`. All legacy-flow and dedicated-activity ownership checks remain unchanged.\n- use `MAIN_LINES` instead of Bash's special `LINES` variable in shell audits, so audit output and budget comparisons cannot be overwritten by the terminal row count.\n\nPhone acceptance after signed build:\n", 'name': 'Document reliable line-count variable'}]

def patch(op, do_apply):
    p = Path(op["file"])
    if not p.exists():
        raise SystemExit(f"FAIL: missing file: {p}")
    text = p.read_text(encoding="utf-8")
    old, new = op["old"], op["new"]
    oc, nc = text.count(old), text.count(new)
    if nc == 1 and oc == 0:
        print(f"SKIP: already applied: {op['name']}")
        return
    if oc != 1 or nc != 0:
        raise SystemExit(
            f"FAIL: FIX3 anchor mismatch for {op['name']}: old={oc}, new={nc}"
        )
    print(f"READY: {op['name']}")
    if do_apply:
        p.write_text(text.replace(old, new, 1), encoding="utf-8", newline="\n")
        print(f"APPLIED: {op['name']}")

check = "--check" in sys.argv[1:]
for op in OPS:
    patch(op, not check)
print("PASS: R7 FIX3 ready/already applied" if check else "PASS: R7 FIX3 applied")
