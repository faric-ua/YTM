# Termux — v1.4.19 bulk account export

Use the full copy-paste block supplied by ChatGPT.

Expected order:
clean repo → pull → temporary unzip → overlay → apply script → manifest → audits → no-deletion check → exact staging → commit → push → signed APK build.
